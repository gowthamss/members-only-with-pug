extends layout 

block content 

    section
        h1 Sign Up

        form(action="/signup" method="post")
            label(for='first_name') First Name:
            input#first_name(type="text" name="first_name" placeholder="First Name" required="true" value=(signup_details===undefined?'':signup_details.first_name))
            label(for="last_name") Last Name:
            input#last_name(type="text" name="last_name" placeholder="Last Name" required="true" value=(signup_details===undefined?'':signup_details.last_name))
            label(for="username") Username:
            input#username(type="email" name="username" placeholder="Username" required="true" value=(signup_details===undefined?'':signup_details.username))
            label(for="password") Password:
            input#password(type="password" name="password" placeholder="Password" required="true")
            label(for="confirm_password") Confirm password:
            input#confirm_password(type="password" name="confirm_password" placeholder="Confirm password" required="true")
            label(for="membership") Are you a member? 
            input#membership(type="password" name="membership" placeholder="Enter the secret code")
            
            button(type="submit") Sign Up

    if errors 
        each error in errors 
            ul 
                li!=error.msg

    if invalidCode 
        p Code is not correct. Please enter the correct code.